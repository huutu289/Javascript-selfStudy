/*
HTML DOM (tiêu chuẩn được W3C đưa ra) là gì?
- là viết tắc của: Document Object Model
- có 3 thành phần
    1. element: tags
    2. attribute: thuộc tính của tags
    3. text: giá trị của tags
các tiêu chuẩn đưa ra cho việc làm web: 
 - 1 tài liệu html phải có đuôi html or htm
 - Doctype trong thẻ html
 - thẻ html ôm hết các thẻ khác (head, body)
    vì sao k phải thẻ body ôm các thẻ còn lại? vì đây là tiêu chuẩn của W3C
 - nếu tạo ra 2 id giống nhau sẽ vi phạm quy tắc của w3c, không tốt cho SEO vì 
   1 website tên id phải duy nhất
 */
//--------------------------------
/*
phân định rạch ròi vời Javascript 
 - DOM không phải của javascript
 - javascript cung cấp bộ công cụ để làm việc trên DOM
 - js có thể làm việc với Browse or với Server (Node JS)
  + đối với Browse: viết mã HTML để brower hiểu, browser cần truy cập vào DOM vì vậy 
    browser dùng DOM API (Application Programming Interface) = WEB API 
    ~ WEB API bao gồm các đối tượng: location (1 đối tượng của WEB API), nhận thông báo hay không?,..
*/